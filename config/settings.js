var settings = {
  database: {
    protocol: "mysql", // or "mysql"
    query: {
      pool: true
    },
    host: "127.0.0.1",
    database: "managment",
    user: "root",
    password: "root"
  }
};

module.exports = settings;